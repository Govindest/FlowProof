# Public Railway backend origin. Safe to expose; contains no secret.
NEXT_PUBLIC_FLOWPROOF_API_URL=https://your-railway-domain.example

# Server-only secret used by Next.js route handlers. Never prefix with NEXT_PUBLIC_.
FLOWPROOF_BACKEND_SHARED_SECRET=replace-with-the-same-random-secret-as-railway
